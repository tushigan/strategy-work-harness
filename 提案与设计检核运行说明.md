# 提案与设计检核运行说明

本说明仅支持 macOS，描述逐字稿、HTML 演示稿和设计返回稿的实际业务操作。脚本、浏览器检查和本地报告不代替策略师内部确认、客户批准、对外发布或返工成效；真实账号是否可用也必须以实际连接和回读证据为准。

Codex 自然语言对话是主入口，策略师交资料、反馈和决定，主控写结构化文件并运行命令，不要求策略师手写 JSON。前置规则见[策略与品牌屋运行说明](策略与品牌屋运行说明.md)和[品牌屋HTML运行说明](品牌屋HTML运行说明.md)。

参考运行组合为 Mac Chrome 153.0.8010.50、Playwright 1.62.1、Node.js 24.19.0，检查视口为1280×900及390×844。接收者仍须核对本机依赖并对当前稿件运行浏览器检查；窄屏长页可能需要纵向滚动，不保证所有 Mac 版本、真实手机硬件或客户现场均可用。

## 在 Codex 怎么说

1. “检查这个内部提案任务的计划、简报和前置成果，告诉我是否可以启动。”主控用 `task-start` 检查有效计划、开始日、未暂停/取消/完成、本任务简报检核及阻断缺口。前置品牌屋任务的完成记录必须有效，包含正文、推导、内部确认及 HTML 检核，不只看两份正文文件。未到开始日只能准备候选内容，不改变原排期。
2. “起草完整、连贯、可以直接念的逐字稿，保留转场和论断来源。”作者完整读本任务简报及计划前置任务中同一任务的品牌屋正文和推导；简报检核通过，正文和推导均已检核并获策略师内部确认。主控回读候选稿后登记，另一个 fresh 独立实例全文检核。
3. 策略师明确说“确认这个任务第几版逐字稿”，或说明退回原因。主控保存真实原话并绑定当前版本及最新报告，不能自己补造确认，也不等于客户批准。
4. “按已确认逐字稿生成 HTML 演示稿，逐页检查后交我单独确认。”先组织页面方案，忠实提炼画面要点，将完整讲者文字保留在备注中，再做内容与视觉独立检核及策略师内部确认。浏览器可以离线翻页、编辑文字草稿和导出副本重开；采纳忠实的画面概括或样式修改时交回 Codex 更新页面方案并重检确认，涉及策略、事实或结论变化时先回逐字稿生成新版。
5. “核对并记录内部提案完成。”`task-complete` 要求有效启动记录、当前简报、逐字稿和演示稿，后两者分别检核并绑定最新有效内部确认，前置完成记录仍有效。此接口只完成内部提案，不能凭简报完成客户确认、产品战略或包装；最后客户批准与实际交付另记。
6. “按这个设计任务的当前简报，逐页检查设计师返回稿，给出可定位的红黄绿意见。”主控登记原件，先显式调用 `design-expression-reviewer` 形成逐页策略诊断候选和视觉证据，再调用不同的 fresh `independent-reviewer` 读取原件、简报、逐页图和诊断候选，重新判断并形成最终报告。策略检核不代替设计总监审美决定、印前检查或法规终审。

角色分工：`project-controller` 显式调用 `proposal-author` 写逐字稿候选、`deck-builder` 准备 HTML 演示稿、`design-expression-reviewer` 形成设计逐页策略诊断候选；不同的 fresh `independent-reviewer` 分别为逐字稿、演示稿和设计稿出正式中立报告，主控回读并登记。角色 TOML 只定义职责，不会自动 spawn 或证明宿主已加载；交接必须包含全部必要原文、版本、任务及实际产物。作者、诊断者和提交者不能改角色名或实例字符串自称独立。

## Agent 现有命令

均从工作包根目录执行，路径相对当前工作包；替换真实任务、提交编号及实例。下面是步骤和格式示意，不是业务记录或批准。

```bash
python scripts/phase5.py status --workspace .
python scripts/phase5.py status --workspace . --refresh
python scripts/phase5.py impact-scan --workspace .
python scripts/phase5.py task-start --workspace . --task ITEM-001-proposal --actor ACTUAL-CONTROLLER
```

`status` 回读产出及设计状态，不写缓存；`status --refresh` 还列任务并刷新 `project/state.json.phase5`，`impact-scan` 同时返回 `artifacts` 和 `submissions`。不能用缓存的“完成”替代真实完成依据。空白模板不为本文示例运行刷新、生成、报告或批准，`project/state.json` 保持原空白状态。

```bash
python scripts/phase5.py script-publish --workspace . --input project/author-input/逐字稿.json --request project/author-input/逐字稿请求.json --author ACTUAL-AUTHOR
python scripts/phase5.py script-sources --workspace . --task ITEM-001-proposal
python scripts/phase5.py script-review --workspace . --task ITEM-001-proposal --input project/reviews/逐字稿检核.json
python scripts/phase5.py script-confirm --workspace . --task ITEM-001-proposal --input project/inputs/逐字稿确认原文.txt --actor 实际策略师
python scripts/phase5.py script-gate --workspace . --task ITEM-001-proposal --request project/author-input/内部确认门禁.json
python scripts/phase5.py deck-generate --workspace . --task ITEM-001-proposal --request project/author-input/演示稿请求.json --author ACTUAL-DECK-AUTHOR
python scripts/phase5.py deck-sources --workspace . --task ITEM-001-proposal
python scripts/phase5.py deck-browser-check --workspace . --task ITEM-001-proposal
python scripts/phase5.py deck-review --workspace . --task ITEM-001-proposal --input project/reviews/演示稿检核.json
python scripts/phase5.py deck-confirm --workspace . --task ITEM-001-proposal --input project/inputs/演示稿确认原文.txt --actor 实际策略师
python scripts/phase5.py deck-gate --workspace . --task ITEM-001-proposal --request project/author-input/内部确认门禁.json
python scripts/phase5.py task-complete --workspace . --task ITEM-001-proposal --actor ACTUAL-CONTROLLER
```

`内部确认门禁.json` 内容为 `{"human":true}`。不带此请求的 `script-gate/deck-gate` 只检查自身检核通过；仍会核对必要上游确认。`script-confirm/deck-confirm` 保存策略师真实确认原文，各自绑定当前版本和最新独立报告。

## 逐字稿输入与上游

以下 JSON 中的指纹和判断均为占位，运行时从当前 sources/版本记录复制实际值；SHA-256 必须为实际64位小写十六进制。输入示例只展示一章，真实逐字稿须完整覆盖提案任务，不能把这一段当成品。

```json
{
  "task_id": "ITEM-001-proposal", "title": "内部提案", "audience": "内部策略评审", "purpose": "解释已确认策略及待验证问题",
  "sections": [{
    "id": "opening", "title": "本次提案要解决的问题",
    "spoken_text": "我们今天先把本次内部提案要解决的问题说清楚，再按已经确认的品牌屋正文和推导逐项解释。每个结论都要回到现有依据；哪些还是假设、下一步怎么验证，也会在对应段落里说明，避免把候选方案讲成客户已经认可的事实。",
    "transition": "先明确问题和依据，再进入各项策略的解释。",
    "claim_refs": [{"key": "ITEM-001-draft::brand-house", "section_id": "positioning"}]
  }]
}
```

每章 `id` 唯一，`title/spoken_text/transition` 为非空文字，`claim_refs` 是非空 `{key,section_id}` 对象列表，指向实际上游章节。`spoken_text` 至少40个非空白字符是机器底线，不证明它是完整逐字稿。`audience` 可为非空文字或非空文字列表。

逐字稿请求只接受恰好三个 `phase3_refs`，不兼容 `dependencies`，不把 `summary` 加入这三项：

```json
{
  "phase3_refs": [
    {"space":"phase3","key":"ITEM-001-proposal::brief","version":1,"sha256":"简报实际指纹"},
    {"space":"phase3","key":"ITEM-001-draft::brand-house","version":1,"sha256":"正文实际指纹"},
    {"space":"phase3","key":"ITEM-001-draft::derivation","version":1,"sha256":"推导实际指纹"}
  ]
}
```

本任务在计划中标题须为“内部提案”；正文与推导须属于同一计划前置任务，三份上游均当前且无阻断缺口。版本登记不等于任务完成，任务完成仍需 `task-start/task-complete` 的有效前置及产出确认。

## 演示稿请求与编辑

请求含 `script/brand-house/derivation/brief` 四个完整引用和必填 `page_plan`；可另带 `visual_style:{path,sha256}` 提供本任务的 CSS 候选，修订时另带 `revision`。`script` 必须是本任务当前、已检核并内部确认的逐字稿，其余三项须与逐字稿绑定的上游完全对应。下面仅示意四个版本引用，完整请求还须加入[页级方案](.agents/skills/html-deck/references/page-plan.md)，由 Agent 编写，不要求策略师填 JSON：

```json
{
  "script":{"space":"phase5","key":"ITEM-001-proposal::proposal-script","version":1,"sha256":"逐字稿实际指纹"},
  "brand-house":{"space":"phase3","key":"ITEM-001-draft::brand-house","version":1,"sha256":"正文实际指纹"},
  "derivation":{"space":"phase3","key":"ITEM-001-draft::derivation","version":1,"sha256":"推导实际指纹"},
  "brief":{"space":"phase3","key":"ITEM-001-proposal::brief","version":1,"sha256":"简报实际指纹"}
}
```

封面保留提案标题，内容页来自忠实提炼后的画面要点及逐字稿 citations，不再机械切分口语段落。支持要点、对照表、零基线横条图和本地静态图片；章节全文及转场自动保留在折叠讲者稿。缺方案、错版本、引文不存在、数值无依据或图片指纹不符均停止。文件中的版本信息仅代表生成当时，不会查询磁盘最新状态，正式使用前由主控再查 gate。

输出：逐字稿为 `project/outputs/proposal/<task_id>/proposal-script.json`、`.md`，历史在 `versions/vNNNN.json`、`.md`。Huashu 先准备当前任务的 CSS，`html-deck` 将样式、已确认内容及逐页映射一起登记成正式 `html-deck/presentation.html` 和 `current.json`；不另造旁路正式视觉稿。样式冻结在 `style-<sha256>.css` 及 `versions/vNNNN.css`，进入必读来源，后续候选修改不改变历史。`data-phase5-deck` 记录逐字稿、三个上游、可选样式引用及逐页映射，不包含未登记的自由视觉派生物。

浏览器缓存只是辅助草稿，页面下载按钮仅导出草稿 HTML；发起下载不等于下载成功，副本重开也不等于正式原文件已更新。本模块不提供品牌屋 HTML 的原文件直接保存及文件内历史编辑能力。仅忠实画面概括变化可修订 page_plan 并重检演示稿；策略意思变化必须回到逐字稿新版、独立检核及内部确认。样式修改也生成新版、重跑浏览器并重新检核确认，不能覆盖登记文件或历史。

新版还将页级方案、完整讲者稿、引用和图片指纹登记到 JSON；图片内嵌于 HTML，并以内容指纹文件和版本快照保留。独立报告 scope 必须含 `content_fidelity`，每页 visual_evidence 必须另写 `content_fidelity`（提炼忠实性）及 `media_observation`（图表/图片检查，无媒体亦注明）。下方通用旧字段示意仅是基础，不替代这两个新增实质判断。

HTML 通过独立检核并单独确认后，使用 `python scripts/deck_export.py export --workspace . --key <task_id>::html-deck` 导出来源绑定的 PDF，再用 `inspect --workspace . --manifest <本次manifest相对路径>` 回读。每次在本任务 `html-deck/exports/<运行编号>/` 生成新文件，不覆盖历史；状态仍是待视觉检核。实际渲染每一页 PDF、对照原文并交独立实例观察后才可按授权交付；HTML 的草稿重开检查不是 PDF 检查。自由布局多文件、概览墙和 PPTX 没有自动接入原生门禁，只能作为另行验收的扩展候选。完整样式及导出限制见 [Huashu 工作包执行说明](.agents/skills/huashu-design/references/harness-deck.md)。

## 共用独立报告

先取得 `script-sources/deck-sources`；`target` 原样复制，通过报告的 `scope` 覆盖全部 `scope_required`，`checked_sources` 必须完整读取并覆盖所有 `checked_sources_required`，包括实际成品、上游和全部来源原文，不以摘要代替。退回或资料不足报告允许部分检查，但 `uncovered` 必须逐项包含缺少来源的 `source:项目相对路径`、缺少章节或检查项的 `scope:实际编号`，另写清未检查原因；仅写一句“资料不足”不能代替遗漏清单。下面只展示字段及定位格式，来源数组须用实际完整清单替换，判断须写实质说明，示例 `passed` 不代表本阶段或任何项目已通过：

```json
{
  "target":{"space":"phase5","key":"ITEM-001-proposal::proposal-script","version":1,"sha256":"当前逐字稿实际指纹"},
  "reviewer_instance":"ACTUAL-FRESH-REVIEWER", "status":"passed", "simulation":false,
  "scope":["opening"],
  "assessment":{
    "task_fulfilment":"任务完成度的实际判断",
    "evidence_reasoning":"证据与论断推导的实际判断",
    "counterevidence":"反证、替代解释和误读风险的实际判断",
    "role_boundary":"作者、检核者及策略师职责的实际判断",
    "uncertainties":"仍需验证的不确定性说明"
  },
  "findings":[{
    "level":"green", "location":"opening 正文及转场", "requirement":"本任务实际要求",
    "evidence":"已读原文及产物的具体定位", "impact":"对任务和听众理解的实际影响",
    "action":"保留或调整的具体建议", "decision_owner":"实际策略师"
  }],
  "uncovered":[],
  "checked_sources":[
    {"path":"project/outputs/proposal/ITEM-001-proposal/versions/v0001.json","sha256":"当前逐字稿实际指纹"},
    {"path":"project/outputs/proposal/ITEM-001-proposal/versions/v0001.md","sha256":"可读稿实际指纹"}
  ]
}
```

`assessment` 五项为非空实质文字；每条 `findings` 除 `level` 外还有 `location/requirement/evidence/impact/action/decision_owner` 六项非空文字。状态仅 `passed/passed_with_yellow/returned/insufficient_evidence`，有红灯或未覆盖范围不能通过，有黄灯只能附黄灯通过。作者参与过该产出任一版本就不能独立检核它；真实项目 `simulation:false`，不得修改 `test_mode` 放行。

报告不得提交系统保留字段 `record_id/created_at/event/evidence/required_scope`，否则拒绝登记；报告原件也不允许通过这些字段改变事件类别或复用旧编号。内部确认同时绑定最新检核编号和报告文件指纹；旧确认缺少指纹时会阻断，须由策略师重新确认，系统不代填、不覆盖旧记录。嵌套过深的 JSON 会以中文报错，不能登记任何检核事件。

演示稿通过报告在上述共用字段上另加以下实际证据；`target/checked_sources` 改取 `deck-sources`，`scope` 覆盖封面、四项检查及全部逐字稿章节。以下演示三页，真实数组必须覆盖每一页：

```json
{
  "scope":["cover","display","navigation","editing","version_binding","opening"],
  "browser_checks":{
    "display":true, "navigation":true, "editing":true, "export_reopen":true, "offline":true,
    "evidence":{"path":"project/reviews/browser/实际运行编号/browser-result.json","sha256":"实际检查文件指纹"}
  },
  "visual_evidence":[
    {"page":1,"rendered":true,"observation":"封面实际观察","method":"实际浏览器打开及截图方法",
     "file":{"path":"project/reviews/visual/deck-page-001.png","sha256":"实际图片指纹"}},
    {"page":2,"rendered":true,"observation":"章节转场页实际观察","method":"实际浏览器打开及截图方法",
     "file":{"path":"project/reviews/visual/deck-page-002.png","sha256":"实际图片指纹"}},
    {"page":3,"rendered":true,"observation":"正文页实际观察","method":"实际浏览器打开及截图方法",
     "file":{"path":"project/reviews/visual/deck-page-003.png","sha256":"实际图片指纹"}}
  ]
}
```

演示稿源码强制逐页 `page/rendered:true/observation/method/file:{path,sha256}`。先运行 `deck-browser-check`，它使用已安装的 Node、Playwright 和 Mac Chrome（Node及包目录可由 `HARNESS_NODE`、`HARNESS_NODE_MODULES` 指定），在隔离离线上下文中逐页检查1280×900及390×844显示、翻页、编辑和导出后无缓存重开，不使用个人浏览器配置。缺依赖、运行失败或版本中途变化均停止，不自动安装或伪造通过。

成功结果位于命令返回的 `project/reviews/browser/<run_id>/browser-result.json`；同目录保留请求、全部截图、导出草稿和重开截图，事件中绑定执行器、当前HTML、目标版本及结果指纹。报告的 `browser_checks.evidence` 必须原样引用命令返回的 `result`，`visual_evidence.file` 取结果中对应页面的实际截图，独立检核者逐页看图再写观察；上述路径示例不是可复用证据。失败目录和旧报告保留，新报告使旧内部确认失效。纯测试夹具须项目、报告和结果都明确模拟，不可代替实际验收。

每张截图须真实解码、指纹一致、至少100x100且非纯色；同一图片改名不能覆盖多页。只解析HTML、复制旧截图或手写五个通过布尔值都不能放行。机器运行证明的是记录的显示与交互检查，不替代业务判断，也不提供抵御有权篡改所有本地脚本和记录者的身份认证。

## 设计提交及报告

PNG/JPG/JPEG 真实解码并核对内容与后缀，一张为一页；PDF 用 `pypdf`、PPTX 用 `python-pptx` 解析只确定结构和页数，须另行实际打开或渲染全部页面。旧 `.ppt` 可归档但 `format:unsupported`，请人工转 PDF/PPTX；其他读取失败为 `format:unreadable` 并保留 `parse_error`。不能声称自动识别缺字体，只有视觉核查能观察字体异常；未可靠渲染就记录限制。

设计提交请求：

```json
{
  "brief":{"space":"phase3","key":"ITEM-002-design::brief","version":1,"sha256":"简报实际指纹"},
  "author_instance":"ACTUAL-DESIGN-AUTHOR"
}
```

`brief` 必须是本任务当前且已独立检核的设计简报版本引用，不能用 `{path,sha256}`，设计字段以 `phase3_document.py` 的 `DESIGN_FIELDS` 为准。提交者用 `--actor`，省略 `author_instance` 时脚本将提交者也视作作者；独立检核者须不同于两者。复制归档原件后使用返回的实际提交编号：

```bash
python scripts/phase5.py design-submit --workspace . --task ITEM-002-design --input project/inputs/设计返回稿.pdf --request project/author-input/设计提交请求.json --actor ACTUAL-SUBMITTER
python scripts/phase5.py design-sources --workspace . --submission DESIGN-ACTUAL-ID
python scripts/phase5.py design-review --workspace . --submission DESIGN-ACTUAL-ID --input project/reviews/设计最终检核.json --author ACTUAL-FRESH-INDEPENDENT-REVIEWER
python scripts/phase5.py design-gate --workspace . --submission DESIGN-ACTUAL-ID
python scripts/phase5.py design-status --workspace . --submission DESIGN-ACTUAL-ID
```

`design-expression-reviewer` 只把逐页策略诊断和视觉证据写成候选，例如 `project/reviews/设计诊断候选.json`；该候选不登记为正式通过，也不直接调用 `design-gate`。随后，未参与设计提交和诊断的 fresh `independent-reviewer` 必须重新读取设计原件、当前简报、`design-sources` 的全部来源、逐页画面和诊断候选，独立填写最终报告。最终报告仍使用现有 `design-review` schema 和 `python scripts/phase5.py design-review` 命令，不新增审批接口；候选诊断作为已读取来源之一列入最终报告的 `checked_sources`，不能照抄候选结论。
设计报告保留共用五项 `assessment` 及六项意见文字，另有 `submission_id/brief_ref/checked_pages/visual_evidence`。`target` 没有 `version`；`checked_sources` 完整取 `design-sources` 的原件、简报、全部来源以及实际读取的诊断候选。以下单页字段合并到共用报告，并替换其目标、范围、意见和来源：

```json
{
  "target":{"space":"design-submission","key":"DESIGN-ACTUAL-ID","sha256":"设计稿实际指纹"},
  "submission_id":"DESIGN-ACTUAL-ID",
  "brief_ref":{"space":"phase3","key":"ITEM-002-design::brief","version":1,"sha256":"简报实际指纹"},
  "scope":["page 1"], "checked_pages":[1],
  "visual_evidence":[{
    "page":1,"rendered":true,"method":"实际打开或渲染方法及环境","observation":"实际画面中的策略表达",
    "file":{"path":"project/reviews/visual/design-page-001.png","sha256":"实际图片指纹"}
  }],
  "findings":[{
    "level":"green","page":1,"region":"正面主信息区","basis_id":"CHECK-001",
    "location":"第1页正面主信息区","requirement":"简报检查项的实际要求",
    "evidence":"该区域实际可观察证据","impact":"对消费者理解的实际影响",
    "action":"保留或调整的具体建议","decision_owner":"实际策略师"
  }]
}
```

通过须 `scope` 覆盖全部 `page N`、`checked_pages` 为全部实际页码、逐页图片证据有效，且意见覆盖所有 `strategy_checks` 的 `CHECK-xxx`。设计每条意见还需整数 `page`、具体 `region`、有效 `basis_id`；红灯只能引用检查项中 `level:red` 的红清单，不能把 yellow 依据、个人审美差异升级为红灯。设计视觉证据的 `method/observation` 必填，`file` 为相对路径及指纹对象，不是文字描述。

不可读且无法确认页码时用 `scope:["file readability"]`、`checked_pages:[]`、`visual_evidence:[]`、`findings:[]`，仍完整核读原件与简报来源，`uncovered` 写明限制，状态为 `returned` 或 `insufficient_evidence`。缺关键简报项、红灯或未覆盖不能通过。有黄灯只能 `passed_with_yellow`；本接口没有 `design-confirm`，设计通过不等于客户批准或包装交付。

## 修订、退回与恢复

逐字稿与演示稿分别报告、分别确认。新版、上游变化、报告原件变化会阻断旧状态；同一版本新增报告也会使旧确认失效，确认需绑定最新检核的 `record_id`。退回可在没有通过报告时记录，使用 `reject` 并保留策略师真实原话。

首次生成不能带 `revision`；后续请求的修订结构如下，完整请求仍保留各自必要上游字段：

```json
{
  "revision":{
    "base":{"space":"phase5","key":"ITEM-001-proposal::proposal-script","version":1,"sha256":"上一版实际指纹"},
    "trigger":"review","reason":"本次修改的实际原因",
    "changes":["opening：实际改动及原因"]
  }
}
```

`base` 精确绑定上一版，`trigger/reason` 为非空文字，`changes` 为非空文字列表，不能写成对象列表。初稿后最多两轮自动修订，每轮后须有有效独立检核；第二轮后停下，保留全部历史及争议。策略师明确追加一轮后执行 `allow-more`，其 `target` 绑定上一版，返回的 `record_id` 填下一版 `revision.allowance_id`；只加一轮，累计 `automatic_round` 不清零，每次再加仍须新授权。改任务名、作者或复制旧内容不能重置次数。

```bash
python scripts/phase5.py reject --workspace . --key ITEM-001-proposal::proposal-script --input project/inputs/退回原文.txt --actor 实际策略师
python scripts/phase5.py allow-more --workspace . --key ITEM-001-proposal::proposal-script --input project/inputs/增加一轮原文.txt --actor 实际策略师
python scripts/phase5.py recover --workspace .
```

演示稿对应 `--key ITEM-001-proposal::html-deck`。上游改变先 `impact-scan`，已有明确更新授权可复用，否则由策略师确认更新范围；每份更新稿重新检核及确认，旧完成依据也需重查。返回历史内容以新版登记，不恢复旧确认。

写入中断的 `recover` 只恢复已准备且指纹一致的本地交易。遇到未登记人工修改时保留 `project/outputs/conflicts/phase5/` 副本并停止，不覆盖，也没有本阶段自动合并或强制恢复命令。移交复制整个工作包及隐藏目录、原文、版本、报告和事件；不依赖原电脑聊天历史、浏览器缓存或全局 Skill 路径。记录是本地版本校验，不提供对恶意篡改的身份认证。
