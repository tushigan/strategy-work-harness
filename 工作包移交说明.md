# 工作包移交说明

本入口只做本地安全审计和目录复制，不刷新业务状态、不删除源文件、不联网、不安装依赖、不替代独立业务检核或客户确认。仅支持空白模板和明确声明的合成测试包，不接受真实客户包。

真实项目的日常交接使用工作包内的 `scripts/project_handoff.py`，它与下面的空白包/合成包审计是两件事：

```bash
python scripts/project_handoff.py inspect --workspace . --json
python scripts/project_handoff.py prepare --workspace . --actor 移交实例 --json
python scripts/project_handoff.py accept --workspace . --actor 接收实例 --manifest-id <移交方返回的manifest_id> --json
```

`inspect` 用于新对话、上下文压缩后恢复和接手前检查；`prepare` 绑定当前项目文件清单、阶段、下一步、待恢复事项、角色调用规则和外部依赖；`accept` 必须绑定移交方返回的 `manifest_id`，在接收方回读后核对文件列表和指纹。每次清单的不可覆盖正文保存于 `project/records/handoffs/<manifest_id>.json`。它发现交接后有变化时会拒绝接收，不会静默覆盖。外部账号、宿主依赖、独立 Agent 能力和真实资料分发授权仍需人工确认。

项目长期记忆、最小身份、独立任务、任务关系和操作回执都在 `project/records/`，会随完整目录一起移交。接收方启动报告会重新读取并核对；这些记录损坏时阻断接收，不能当作“没有记录”。本地项目记忆随工作包迁移，但不会因此自动上传云端大脑。

交接目录、祖先目录、清单、事件和锁文件不得使用符号链接，包括指向包内的链接；发现后会在写锁和文件写入前阻断。全部历史清单都须与准备事件逐一匹配，较早版本缺失或冲突不能只靠最近版本正常而放行。仅最近历史副本缺失、但最近清单与准备事件仍一致时，可重新 `prepare` 从原字节补回；直接 `accept` 仍拒绝缺失。不要删除冲突记录来绕过检查。

## 使用方式

在工作包根目录运行，使用 Python 3.11 以上版本：

```bash
python3 -B scripts/package_audit.py audit --root . --mode template --json
python3 -B scripts/package_audit.py prepare-copy --root . --mode template --target ../待移交副本 --json
```

合成包把 `--mode template` 改为 `--mode synthetic`。目标必须不存在，且不能在源目录内；父目录须已存在。失败退出码为 1，命令用法错误为 2。请保留输出中的错误和黄灯，不只看退出码。

`audit(root, mode="template")` 返回 `ok`、`errors`、`warnings`、`inventory`、`excluded`、`external_dependencies` 和 `references`。诊断只含问题类别、相对文件位置和行号，不输出凭据原值或解析异常原文。

`prepare_copy(root, target, mode="template")` 先审源目录，成功才在目标旁建立隔离暂存目录。复制全部有效文件和相对符号链接，不跟随链接；副本复审、逐项 SHA-256 核对、清单写入回读都成功后才公布目标目录。失败清理本次暂存，不覆盖已存在的目标，不改源目录；修好原因后可用原目标名重试。

副本根目录的 `package-inventory.json` 保存内容清单和原始排除记录，不保存开发机路径和时间戳。清单不包含自身，以免循环计算。复制相同内容可得到相同清单；再次审计能检测清单所列文件的改变。清单不是数字签名，不能抵抗有权重写整个包的人。

## 必须检查的内容

- 空白模板必须 `status=not_initialized`，不能夹带业务身份、任务、检核、确认、导入或历史数据。必需运行文件缺失时正常报错，不以“稍后补齐”放行。
- 包内必须有 14 个业务 Skill、六个策略工作角色和对应调用规则；其中 `huashu-design` 负责视觉样式候选，`html-deck` 把样式和正文登记到同一份正式 HTML 并检核，确认后可导出 PDF 待检副本。不包含开发测试、开发角色、开发计划及其他系统入口。角色 TOML 只定义职责，接收者须验证宿主可用实例与主控显式调用，不能凭文件存在宣称自动运行。
- 合成包必须 `test_mode` 为布尔值 `true`，并逐文件提供下面的资料声明。仅文件名带“合成”或仅打开测试开关均不够。
- 凭据文件、私钥、实际密钥值、开发机绝对路径、越界引用和外链令牌会阻断。报告不回显命中的原文。Python 检测正则通过语法树识别，不把检测规则自身当成密钥或开发机路径。
- JSON、JSONL、TOML 中的本地文件引用必须存在。明确 `required:false` 的缺失引用保留提示；远端链接另列为外部引用，不冒充已缓存到包内。方法来源中的原技能 `SKILL.md` 是出处，不要求接收者安装原全局技能。
- 包内程序相互导入、Agent 配置引用和必需模板属于包内依赖；宿主工具、第三方运行库和授权属于外部依赖，两者不能混淆。
- 只排除 `.DS_Store`、`__pycache__` 目录及其直接的 `.pyc` 缓存，并在结果里逐项列出。缓存目录藏有其他文件会阻断；不默默排除失败报告、历史版本、黄灯、冲突副本或待处理事项。源目录已有缓存不会被删除。
- `.venv`、`venv`、`node_modules`、个人凭据及浏览器配置不得随包。未知二进制即使声明过也不自动放行。已知媒体还须绑定指纹并记录实际核对方法；自生成合成资料可以记录原文和来源比对，但不能冒充人类审核。自动审计不等于读懂图片、音视频或扫描件，人工检查未验证的提示保留。

## 合成资料声明

在合成测试包内提供 `project/records/package-materials.json`，由负责资料的人确认后生成，不在空白源模板内预填。`materials` 至少包括状态文件以及所有业务原文、数据、报告、事件、版本、图片和附件；每项使用实际内容指纹，不用通配符。声明文件自身不列入，避免循环指纹。

```json
{
  "schema_version": 1,
  "classification": "synthetic",
  "contains_real_client_data": false,
  "statement": "全部资料为本次隔离测试生成，不含真实客户资料；确认仅为模拟。",
  "materials": [
    {
      "path": "project/state.json",
      "sha256": "填写实际文件的64位SHA-256",
      "classification": "synthetic",
      "reviewed": true,
      "review_note": "记录实际核对范围，不得自动编造已检查。"
    }
  ]
}
```

每份资料都必须列出 `path`、`sha256` 和 `classification`；必需资料漏列、指纹过期、真实客户标记或格式未知都会阻断。纯文本条目的 `reviewed` 和 `review_note` 建议填写，但不是机器放行的必填项；已知媒体还必须有 `reviewed:true` 和非空 `review_note`。上面的示例给出了完整写法，不代表所有格式都执行同样的审阅字段校验。

更改文件后须重新检查并更新声明，不能只改指纹假装完成内容核查。审计无法凭文字判断声明者是否说真话；不得将已知真实客户数据谎报为合成。
`reviewed: true` 只表示 `review_note` 中所述检查确实完成，不等于独立业务检核或人工批准。自动生成的说明须明确机器检查的范围和未做的人工检查；审计通过仍保留媒体检查与声明真实性的黄灯。

## 依赖清点

以下块供审计器读取。声明运行依赖不代表当前电脑已经安装或已经授权；本入口只用 Python 标准库，不导入被审计包的代码。

```package-dependencies
{
  "schema_version": 1,
  "external_dependencies": [
    {"id": "python", "kind": "runtime", "version": ">=3.11", "purpose": "本地脚本与只读审计"},
    {"id": "node", "kind": "runtime", "version": ">=20", "purpose": "表格与浏览器辅助检查"},
    {"id": "openpyxl", "kind": "python", "module": "openpyxl", "version": "3.1.5", "purpose": "Excel只读核对"},
    {"id": "pypdf", "kind": "python", "module": "pypdf", "version": "6.10.0", "purpose": "PDF结构读取"},
    {"id": "Pillow", "kind": "python", "module": "PIL", "version": "recipient-verified", "purpose": "图片实际解码"},
    {"id": "python-pptx", "kind": "python", "module": "pptx", "version": "recipient-verified", "purpose": "PPTX结构读取"},
    {"id": "playwright-python", "kind": "python", "module": "playwright", "version": "recipient-verified", "purpose": "Huashu Python 路线的浏览器渲染"},
    {"id": "openai-whisper", "kind": "python", "module": "whisper", "version": "optional", "purpose": "仅探测，不自动转写"},
    {"id": "@oai/artifact-tool", "kind": "node", "version": "host-provided", "purpose": "Excel制作"},
    {"id": "playwright", "kind": "node", "version": "recipient-verified", "purpose": "浏览器验收"},
    {"id": "pdf-lib", "kind": "node", "version": "1.17.1", "purpose": "Huashu HTML Deck 转 PDF"},
    {"id": "pptxgenjs", "kind": "node", "version": "4.0.1", "purpose": "Huashu 可编辑 PPTX 路线 A"},
    {"id": "sharp", "kind": "node", "version": "0.34.5", "purpose": "Huashu 缩略图和 PPTX 图像处理"},
    {"id": "requests", "kind": "python", "module": "requests", "version": "recipient-verified", "purpose": "Huashu 可选云端视频审阅，不是本地 Deck 默认依赖"},
    {"id": "Chrome/Chromium", "kind": "runtime", "version": "recipient-verified", "purpose": "本机浏览器检查"},
    {"id": "ffmpeg", "kind": "command", "version": "optional", "purpose": "仅探测音视频能力"},
    {"id": "Codex", "kind": "host", "version": "recipient-verified", "purpose": "执行与独立实例能力"},
    {"id": "lark-cli", "kind": "connector", "version": "recipient-verified", "purpose": "飞书能力，须另行授权"},
    {"id": "shared-memory", "kind": "connector", "version": "recipient-verified", "purpose": "云端大脑能力，须另行授权"}
  ],
  "bundled_assets": [
    {
      "path": ".agents/skills/project-import-template/assets/openclaw-gantt-import-template.xlsx",
      "sha256": "ae3c8d041ff8aa9a2fcc48a7ceb32d2c0b7e54950b4a307a132b7e19f5a0d59d",
      "classification": "reference_template",
      "reviewed": true,
      "review_note": "既有导入格式基线，仅模板参考内容，不是已采纳的客户业务记录。"
    }
  ]
}
```

Python 依赖版本以包内 `requirements.txt` 和本说明共同清点；设计稿读取额外使用 Pillow、python-pptx，接收者还须安装核验，不能只运行基础环境检查便宣称全功能就绪。Node 依赖由宿主提供，必要时在进程环境设置 `HARNESS_NODE`、`HARNESS_NODE_MODULES`，不把它们的本机值写进包。

原生演示稿的 PDF 入口为 `scripts/deck_export.py`，使用宿主 Node Playwright、pdf-lib、已安装的 Mac Chrome 与 Python pypdf；它不自动安装依赖。Huashu 上游保留的多文件/`deck-stage` 导出器不适用于原生模板，且其裸 ESM 导入不读取 `HARNESS_NODE_MODULES`。仅在明确制作扩展候选时，才在实际 Skill 目录按其 `package-lock.json` 安装和核验依赖；Python PPTX 路线另验 Python Playwright 与浏览器。不能凭 Node 浏览器成功就声称所有格式已可用。

接收者先审计清单，再检查环境；缺飞书或云端权限应继续本地能做的工作，保留外部资料缺口，不编造连接成功。新目录恢复是读取状态和记录仍完整，不代表业务门禁全通过。正式继续工作时按 `AGENTS.md` 逐阶段回读，不能沿用旧版批准。

本入口仅面向 Mac 的本地审计与复制。凭据探测不是完整恶意软件检测，文件清单也不替代资料的保密审查或分发授权。复制时应停止其他程序写源包；检测到内容变化会停止，不能保证与恶意并发修改者互相隔离。
