# 策略与品牌屋运行说明

本说明用于策略与品牌屋工作。策略师通过 Codex 提供资料、反馈和决定，主控整理结构化输入并运行命令，不要求策略师手写 JSON。命令只处理当前工作包，不自动代表业务确认、客户批准或外部上传。

## 角色分工

`project-controller` 由主控显式调用 `strategy-author` 撰写任务简报、研究分析和品牌屋候选稿。作者只写主控划定的候选文件，不登记公共版本、不确认自己的产出。主控回读并登记后，再把完整原文交给未参与该产出的 fresh `independent-reviewer`，由其出正式独立报告；作者的自检或脚本校验都不能替代这一步。

角色 TOML 只是职责定义，不证明宿主已经自动加载或真实调用了这些角色。若实际环境没有对应角色名，主控必须如实保留待检状态，不能只改实例名称冒充独立检核。

## 正常推进

1. 读取已确认的 Phase 2 任务计划，执行 `python scripts/phase3.py status --workspace . --refresh`。到期不等于可以执行；前置未确认、暂停、取消、已完成或缺关键简报时不自动启动，原承诺日期不变。
2. 主控显式调用 `strategy-author`，让其完整读取任务、来源和项目 Skill，写本任务简报候选。主控登记版本，交未参与撰写的 fresh `independent-reviewer` 检核。
3. 调研简报通过后，用 `start` 启动已有调研任务；主控继续显式调用 `strategy-author` 产出访谈提纲、证据稿和研究分析候选，分别登记并交 fresh `independent-reviewer` 检核。线下采集由人完成；输入不足只做明确标注的假设分析和准备工作。
4. 策略师提供研究分析版本的内部确认后，用 `complete` 记录调研节点。品牌屋任务必须有自己的简报，前置研究有效后才启动。
5. 前置研究有效后，主控显式调用 `strategy-author` 生成品牌屋正文和独立推导候选；两者分别登记、交 fresh `independent-reviewer` 检核，并分别取得策略师内部确认，才能完成“品牌屋初稿”任务。不以首次生成替代内部确认。
6. 内部提案、设计提交、客户确认和实际设计交付由后续运行说明处理；本说明只登记当前阶段真实存在的简报、研究和品牌屋产出。

## 全文来源

```bash
python scripts/phase3.py source --workspace . --input project/inputs/访谈.md --metadata project/inputs/来源信息.json --actor 真实登记者
```

元数据字段：`source_id`（稳定编号）、`title`、`owner`、`collected_at`（YYYY-MM-DD）、`origin`、`scope`、`project_id`、`tier`、`relation`。`tier` 为1高层/内部、2现场/经销商、3公开/行业；第3层必须有 `external_locator`（网址或出版定位）。`relation` 为 `current_project` 或 `method_reference`，后者不能作为本项目事实证据。

支持 UTF-8 TXT、MD、SRT、VTT、CSV、文字 PDF、XLSX/XLSM。原件归档在 `project/inputs/evidence/`，全量文字及定位在 `project/research/sources/`。不执行宏或公式，不静默截取长资料。PDF 图像或 Excel 公式造成不可直接引用时，可对明确页码或单元格范围补可靠文字，并登记原件指纹、核验人、核验动作和理由；只有已核验范围可用，不会整份自动放行。操作见[资料核验](.agents/skills/research-evidence/references/资料核验.md)。不支持自动 OCR，也不能用一句“已看”消除未覆盖问题。

研究结论按主张类型、适用范围和反证判断，不按来源层数投票。内部资料可以支持限定的内部事实或管理层意图，但不能自动证明市场认可；纯案头任务可完成公开资料范围内的比较，不因未走访就全部停工。不能支持的主张单独列缺口，保留仍可推进的工作。

同一 `source_id` 登记新文件会新增来源版本，旧原件保留，下游变为待对齐。文件被直接改动或丢失也会被识别。

## 候选内容与登记

```bash
python scripts/phase3.py scaffold --workspace . --task ITEM-001-research --kind brief
python scripts/phase3.py publish --workspace . --input project/author-input/简报.json --request project/author-input/依赖.json --actor 真实作者实例ID
```

`scaffold` 只返回未填写的技术模板，不是产出。作者依据完整资料填写；空标题或正文不能登记。

每份正文包含 `task_id/kind/title/mode/sections/gaps/questions`。`mode` 只能是 `draft` 或 `hypothesis`，不由作者宣布已批准。章节的 `id` 和顺序以 `scripts/phase3_document.py` 的 `SECTIONS` 为准，每章含 `title/text/evidence/counterevidence/assumptions/verification`。证据数组用证据稿中的编号；假设同时写验证方法。少量启发问题最多5条，不能反复问已有明确答案。

`gaps` 每条含 `question/cannot_do/can_do/blocks_completion`。不要混淆“无法声称消费者已认可”和“无法完成内部假设稿”，但也不能把确实阻断本任务的缺口写成无阻断。

设计简报的 `brief_type` 支持 `brand_tone_upgrade/packaging/terminal_material/poster`，按 `DESIGN_FIELDS` 填策略转换清单；普通简报是 `research/strategy/copywriting`。不规定构图、色值或字体。

证据稿额外有 `entries` 和 `conflicts`。每个条目含 `id/source/location/quote/classification/statement/limits`；来源引用复制当前记录，逐字引文必须与原文位置一致。`classification` 为 `fact/opinion/inference/hypothesis/future_capability`。矛盾记录含 `evidence_ids`（至少两个）及 `analysis`，不按层级投票。

依赖请求文件示意：

```json
{
  "dependencies": [
    {"space":"phase3","key":"ITEM-001-research::brief","version":1,"sha256":"实际指纹"}
  ]
}
```

计划引用由脚本绑定；来源引用的 `space` 为 `source`，`key` 是来源编号。非简报产出直接引用本任务简报；分析引用证据稿，品牌屋引用计划指定前置任务的分析，推导引用同任务品牌屋。章节证据编号必须来自依赖中的证据稿。

稳定文件在 `project/briefs/<task_id>/brief/`、`project/research/<task_id>/<kind>/`、`project/outputs/brand-house/<task_id>/<kind>/`。`current.json` 为当前结构化稿，同名 Markdown 为可读稿，`versions/` 保存不可覆盖历史。相同内容和依赖不会重复建版。

## 独立检核和内部确认

```bash
python scripts/phase3.py review-sources --workspace . --key ITEM-001-research::brief
python scripts/phase3.py review --workspace . --key ITEM-001-research::brief --input project/reviews/独立报告.json
python scripts/phase3.py confirm --workspace . --key ITEM-001-research::analysis --input project/inputs/策略师确认.txt --actor 实际策略师
python scripts/phase3.py start --workspace . --task ITEM-001-research --actor 总控实例ID
python scripts/phase3.py complete --workspace . --task ITEM-001-research --actor 总控实例ID
```

独立报告须含：精确 `target`、真实 `reviewer_instance`、`status`、已检查章节ID `scope`、`findings`、`uncovered`、`checked_sources`、`simulation:false`，以及 `assessment`：
`task_fulfilment/evidence_reasoning/counterevidence/role_boundary/uncertainties` 五项实质说明。字段存在不证明推导正确，主控仍须核读独立实例的实际判断。

状态仅为 `passed/passed_with_yellow/returned/insufficient_evidence`。问题包含 `level`（red/yellow/green）、`location/requirement/evidence/impact/action/decision_owner`。红灯或未覆盖不能通过；有黄灯须使用附黄灯通过，不把合理分歧判错。

作者不能自审自己参与过的该产出。通过报告必须覆盖 `review-sources` 的所有文件及原文指纹。上游退回、新版本或文件变化都会使旧通过失效。内部确认仅代表策略师提供的线下内部确认，不是客户批准。`reject` 可记录退回。

## 只提交反馈即可

```bash
python scripts/phase3.py feedback --workspace . --input project/inputs/反馈.txt --actor 实际提供者
```

自动选择只在候选唯一时发生。可从原文中的任务编号、文件路径、品牌屋/简报等类型与 v1/第1版定位；多个候选则保存歧义，不改稿，问清版本。必要请求包含 `target`、`feedback_role`（strategist/director/client）。`bind-feedback` 使用请求中的 `feedback_id/target` 与用户澄清原文。

语音可归档 MP3/WAV/M4A/AAC/OGG/FLAC/MP4/WEBM，但本阶段没有已验证自动转写。`capabilities` 显示实际工具检查；缺可靠转写时保存为 `needs_transcript`。策略师可补 TXT/MD/SRT/VTT，保留时间点和不确定说话人，在请求中指定 `transcript`。不会声称已经听取音频，不自动接付费服务。

作者先按反馈改候选正文，再在同一次 `publish` 请求中附：
`revision.base`（旧当前引用）、`trigger`（feedback/review/alignment）、`event_id`、`reason`、`section_reasons`（每个改动章节ID的原因）。输出自动保留新旧对照，正文的 `questions` 保留少量启发问题。旧版本反馈不能直接修改另一版，先核对再重定目标。

初稿之后最多两轮自动修订，每轮后必须有有效独立复核才能再改下一轮；`director_rework` 单独标记由总监反馈触发的次数。脚本重试不计业务轮次，不能换作者或名字清零。需继续时用实际决定原文执行 `decision`，请求 `action:allow_more`，累计次数仍保留。后续版本保留早期反馈、转写、澄清及追加轮次授权的指纹，依据变化会阻断使用。模拟批准只允许隔离测试，不得启用到真实项目。

## 上游变化、人工修改和恢复

上游变化后先用 `status --refresh` 列出直接和间接受影响产出。已有授权可记录为 `decision` 的 `action:align`，否则先取得更新范围。修订请求绑定该决定；更新后的每份稿都重新检核、必要时重新确认，不替下游继承批准。

人工修改 `current.json` 或 Markdown 时，脚本保留冲突副本并停止。比较后若策略师决定恢复登记稿，保存决定原文，用 `decision` 的 `action:restore_registered` 登记，再用 `restore-registered` 和 `decision_id` 执行。决定后文件再次变化会再停止。人工稿永久保留；要采纳的内容由作者整理进新的候选 JSON 后登记，不能修改历史快照。

返回历史内容时，读取旧版作为候选，绑定当前版本、有效上游和明确的对齐决定，形成新版本并重新检核。不会恢复旧批准。

写入中断时执行 `recover`；它只恢复已准备且指纹一致的交易，不伪造完成。快照先于交易产生的孤立文件也保留，后续跳过其版本号。整目录复制后重新运行启动/状态检查即可继续；不要只交接最终 Markdown。

首次登记尚未完成就遇到人工修改时，没有可用的已登记版本。先保留冲突并核对待完成交易，策略师同意继续后使用 `decision`，请求 `action:resume_pending`，再用 `recover --request 决定引用.json`，其中包含 `decision_id`。决定绑定交易及当时两个文件的指纹；决定后再次改动会停止。此方式也适用于后续交易冲突，不能在未获同意时替用户恢复。事件和交易准备记录采用原子替换；已有损坏事件不会被追加掩盖，需保留原件核对。

代码与记录是本地工作流，不提供抗恶意篡改的身份认证。本工作包仅支持 macOS。原生 Office、真实语音转写、外部连接、客户批准和返工成效都必须在实际项目中另行核对；本地脚本、合成资料或作者自报不能替代这些业务证据。
